# Required: Discord Bot Token
DISCORD_BOT_TOKEN=YOUR_TOKEN

# Docker user/group ID
PUID=1000
PGID=1000

# Optional: Bot-level superadmin IDs (comma-separated, bypass all checks)
# ADMIN_IDS=123456789,987654321

# Optional: SQLite database path (default: data/layer_vote.db, relative to the
# working dir). In Docker this resolves to /app/data/layer_vote.db via the volume.
# DB_PATH=data/layer_vote.db

# Optional: Debug logging
# DEBUG_MODE=true

# Optional: Custom layer data source URL(s)
# Accepts a single URL, a comma-separated list, or a JSON array.
# When the same rawName appears in multiple sources, the source listed later wins.
#   Single:   LAYERS_JSON_URL=https://a.com/layers.json
#   List:     LAYERS_JSON_URL=https://a.com/x.json,https://b.com/y.json
#   JSON:     LAYERS_JSON_URL=["https://a.com/x.json","https://b.com/y.json"]
LAYERS_JSON_URL=["https://raw.githubusercontent.com/fantinodavide/SquadLayerList/refs/heads/main/layers.json","https://raw.githubusercontent.com/fantinodavide/SquadLayerList/refs/heads/main/mods/supermod/layers.json"]

SQUADCALC_BASE_URL=https://beta.squadcalc.app
